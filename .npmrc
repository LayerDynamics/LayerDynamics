; Hoist tslib to the root so transitive CJS deps (e.g. recast, pulled in by the
; Storybook/Vitest browser-test toolchain) can resolve it. recast@0.23.11
; declares tslib but pnpm's strict store layout otherwise leaves it unresolvable.
public-hoist-pattern[]=tslib
public-hoist-pattern[]=*tslib*
