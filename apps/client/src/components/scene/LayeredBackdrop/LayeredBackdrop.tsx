import LayeredBackdropContainer from './LayeredBackdropContainer'

/** The persistent depth field: drifting starfield + soft hero glow, with
 *  pointer parallax. */
export default function LayeredBackdrop() {
  return <LayeredBackdropContainer />
}
