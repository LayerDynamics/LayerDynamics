// The Ender 5 GLB component + scrub helper live here; the printer rig that uses
// them is `components/scene/Printer`. (There is no standalone printing level — the
// hero is the printer.)
export { Model as Ender5Pro } from './Ender5Pro'
