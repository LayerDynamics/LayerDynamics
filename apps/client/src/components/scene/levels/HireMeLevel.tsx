/**
 * Contact ("hireMe") level — terminal. Its visible content is the real Hire-Me
 * form, rendered as a DOM overlay on the landing (see `HireMeOverlay`, mounted in
 * `routes/Landing`), shown over the persistent `LayeredBackdrop`. Nothing is
 * drawn in-canvas here, so the level renders nothing; it exists as the final
 * scroll stop that the overlay keys off of.
 */
export default function HireMeLevel() {
  return null
}
